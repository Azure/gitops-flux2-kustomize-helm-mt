apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: podinfo
  namespace: cluster-config
spec:
  chart:
    spec:
      version: "6.2.2"
  test:
    enable: false
  values:
    ingress:
      # className: "azure\/application-gateway"
      annotations:
        kubernetes.io/ingress.class: "azure/application-gateway"
        appgw.ingress.kubernetes.io/rewrite-rule-set: rr-cecabf5835a0ee1ae3ffec58ef475c20
      hosts:
        - paths:
            - path: /
              backend:
              pathType: Exact