apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: podinfo
  namespace: cluster-config
spec:
  chart:
    spec:
      version: "6.2.0"
  values:
    ingress:
      hosts:
        - paths:
            - path: /
              pathType: ImplementationSpecific